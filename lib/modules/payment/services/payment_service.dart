import 'package:petapp/core/services/base_api_service.dart';

class PaymentService extends BaseService {
  // Add payment related API calls here

  /*
  Future<BaseApiResponse<dynamic>> processPayment() async {
    return await safeRequest(
      request: () => api.post('/payment/process'),
      fromJson: (json) => json,
    );
  }
  */
}
